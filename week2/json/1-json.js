// JSON is a standard for encoding JavaScript objects as plain text (strings).
// All HTTP communication is based on text transfers. The JSON format was
// created to enable the transfer of JavaScript objects across the Internet by
// encoding them in JSON at the sending side and decoding them to a
// corresponding JavaScript object at the receiving side.

const jsonString = '{"id": 4,"title": "Using APIs"}';

console.log(1, typeof jsonString);
console.log(2, jsonString);
